
import { View, Text,TouchableOpacity, StyleSheet } from 'react-native';
import React from "react";

const ReadMore = ( ) => (
    <TouchableOpacity>
    <Text style={{ fontSize: 15 }}>{readMore.replace('\\n', '\n')}</Text>
  </TouchableOpacity>
);
const styles = StyleSheet.create({
  
});
export default ReadMore; 